# 第四轮对比分析报告

## 修复完成的高优先级问题

| # | 文件 | 问题 | 状态 |
|---|---|---|---|
| 1 | `mod.rs` | `normalize_and_validate_additional_permissions` 缺少 `approval_policy` 参数，缺少 OnRequest 检查 | ✅ 已修复 |
| 2 | `mod.rs` | `resolve_workdir_base_path` 路径解析不规范化（`../` 不处理） | ✅ 已修复 |
| 3 | `shell_command.rs` | 缺少 `is_known_safe_command` 检查（`is_safe_command` 方法） | ✅ 已修复 |

## 修复完成的中优先级问题

| # | 文件 | 问题 | 状态 |
|---|---|---|---|
| 5 | `unified_exec.rs` | 不支持 model 指定 shell 路径 | ✅ 已修复（shell 参数已支持） |
| 6 | `unified_exec.rs` | 缺少 `normalize_and_validate_additional_permissions` 安全检查 | ✅ 已修复 |
| 8 | `apply_patch.rs` | 缺少 patch 结构验证（Begin/End Patch 检查） | ✅ 已修复 |
| 9 | `apply_patch.rs` | `intercept_apply_patch` 改为 async 函数，返回 `Option<Result>` | ✅ 已修复 |
| 13 | `presentation_artifact.rs` | 缺少实际 sandbox policy 检查（path_is_allowed） | ✅ 已修复 |
| 14 | `presentation_artifact.rs` | 缺少 `required_path_accesses` 路径提取 | ✅ 已修复 |

## 确认为架构差异（非错误）

| # | 文件 | 说明 |
|---|---|---|
| 4 | `shell.rs` | `LocalShell` payload 在 Mosaic 简化架构中通过 JSON 统一处理，功能等价 |

## 未修复的低优先级问题

| # | 文件 | 说明 |
|---|---|---|
| 7 | `unified_exec.rs` | 输出格式差异（简单 JSON vs 结构化文本） |
| 10 | `view_image.rs` | 路径解析和 content items 生成方式差异 |
| 11 | `view_image.rs` | 缺少 ViewImageToolCall 事件发送 |
| 12 | `grep_files.rs` | search_path 类型差异（&str vs &Path） |
| 15 | `mcp_resource.rs` | ResourceWithServer 字段手动列出 vs flatten |
| 16 | `mcp_resource.rs` | from_single_server 签名差异（功能等价） |
| 17 | `dynamic.rs` | 架构差异（invoke/resolve 机制 vs session pending） |
| 18 | `search_tool_bm25.rs` | 纯 stub，缺少 bm25 crate 集成 |
| 19 | `multi_agents.rs` | 纯 stub，缺少 agent 子系统 |
